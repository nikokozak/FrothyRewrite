-- Persistence
-- Saves cells and words, restores them, and reads the saved values back.
-- @tag host

tally is cells: 1
set tally[0] to 7
to tally-plus with n [ tally[0] + n ]
save
clear
restore
tally[0] -- => 7
tally-plus: 5 -- => 12
