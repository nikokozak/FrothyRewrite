## Connect

**Frothy: Connect** opens a live session to the board — the status bar item
shows the connection state from here on, and clicking it toggles the
connection.

If more than one serial device is plugged in, a picker asks which port to
use. To skip discovery entirely, set the `frothy.port` setting; to connect
automatically whenever a Frothy file opens, enable `frothy.autoConnect`.
